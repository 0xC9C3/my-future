export type Respondent = {
    active: boolean;
    // eslint-disable-next-line camelcase
    created_at: string;
    id: number;
    email: string;
    name: string;
    // eslint-disable-next-line camelcase
    updated_at: string;
    // eslint-disable-next-line camelcase
    user_id: number;
    key: string;
};
