export type User = {
    // eslint-disable-next-line camelcase
    created_at: string;
    email: string;
    // eslint-disable-next-line camelcase
    email_verified_at: string | null;
    id: number;
    // eslint-disable-next-line camelcase
    updated_at: string;
};
